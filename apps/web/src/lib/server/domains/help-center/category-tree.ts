/**
 * Pure helpers for walking the help center category hierarchy.
 *
 * Implementation has moved to lib/shared so client code can import it.
 * Server code continues to import from this path unchanged.
 */
export * from '@/lib/shared/help-center-tree'
